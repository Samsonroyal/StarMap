Place glTF/glb models here if you have them. The renderer will attempt to load models by name (e.g. models/earth.glb) and fall back to a colored sphere if no model is present.

Recommended filenames:
- earth.glb
- mars.glb
- jupiter.glb

For best performance, use optimized, Draco-compressed glb files and low-resolution textures.
